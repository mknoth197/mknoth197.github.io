<section class="harness-map" aria-labelledby="the-harness-map">
  <header>
    <h2 id="the-harness-map">The practical harness map</h2>
    <p>A practice-derived inspection lens, not a validated or exhaustive taxonomy.</p>
  </header>
  <ol>
    <li>
      <h3><span aria-hidden="true">01 · </span>Guides</h3>
      <p>Instructions · context · skills</p>
    </li>
    <li>
      <h3><span aria-hidden="true">02 · </span>Controls</h3>
      <p>Permissions · hooks · policy</p>
    </li>
    <li>
      <h3><span aria-hidden="true">03 · </span>Sensors</h3>
      <p>Tests · CI · review</p>
    </li>
    <li>
      <h3><span aria-hidden="true">04 · </span>Compounding</h3>
      <p>Rules · tests · reusable workflows</p>
    </li>
  </ol>
</section>

<style>
  .harness-map {
    margin: 2.75rem 0;
  }

  .harness-map header {
    margin-bottom: 0.75rem;
  }

  .harness-map header h2 {
    font-family: var(--font-serif);
    font-size: clamp(1.8rem, 7vw, 2.35rem);
    font-weight: 400;
    letter-spacing: -0.015em;
    line-height: 1.15;
    margin: 0 0 0.65rem;
  }

  .harness-map header p {
    color: var(--fg-dim);
    font-family: var(--font-mono);
    font-size: 0.68rem;
    letter-spacing: 0.06em;
    margin: 0;
    text-transform: uppercase;
  }

  .harness-map ol {
    border: 1px solid var(--editorial-border);
    border-radius: 6px;
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    list-style: none;
    margin: 1rem 0 0;
    overflow: hidden;
    padding: 0;
  }

  .harness-map li {
    margin: 0;
    min-width: 0;
    padding: 1.5rem;
  }

  .harness-map li:nth-child(even) {
    border-left: 1px solid var(--editorial-border);
  }

  .harness-map li:nth-child(n + 3) {
    border-top: 1px solid var(--editorial-border);
  }

  .harness-map h3 {
    color: var(--accent);
    font-family: var(--font-mono);
    font-size: 0.8rem;
    font-weight: 500;
    letter-spacing: 0.08em;
    margin: 0 0 0.45rem;
    text-transform: uppercase;
  }

  .harness-map li p {
    color: var(--fg-muted);
    font-size: 0.9rem;
    margin: 0;
  }

  @media (max-width: 720px) {
    .harness-map ol {
      grid-template-columns: minmax(0, 1fr);
    }

    .harness-map li {
      padding: 1.1rem;
    }

    .harness-map li:nth-child(even) {
      border-left: 0;
    }

    .harness-map li + li {
      border-top: 1px solid var(--editorial-border);
    }
  }
</style>
